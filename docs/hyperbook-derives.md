# Conceptual dérive tests — full-book draft

These are data-level simulations, not added interface behavior. They show the kind of explanation a later Experience Graph must retain for every offered encounter.

## Berlin: the same core, different actions

1. **Core Berlin; many historical layers appear.** Offer `encounter:027` (p. 34), which describes beginning at present-day Berlin and drilling into historical layers. The basis is direct spatial evidence for Berlin plus the editorial interactional cue `core-depth`.
2. **Descend to the 1926 map.** Offer `encounter:042` (p. 56), on zoom and things absent from the Pharus map. This is a temporal/cartographic resonance, not a claim that every Berlin map represents the same absences.
3. **Align the selected historical raster to the basemap.** Offer canonical `encounter:158` (p. 111), on alignment producing a relation. The reason is cartographic alignment, independent of Berlin geography.
4. **Zoom out, then return to Berlin.** Offer `encounter:050` (p. 64), on Anhalter time-layers opening downward and laterally. The direct Berlin relationship, `revisit`, and `layer` affordances all exist; cooldown blocks the earlier source passages.

The test does not cycle one flâneur quotation. The flâneur remains available (`encounter:019`, p. 23), but only when it is novel and materially relevant.

## Los Angeles: beyond Ghost Maps

1. **Core downtown Los Angeles and choose a historical layer.** A direct Los Angeles/ghost-map encounter can appear, for example canonical `encounter:156` (p. 69), but only if the selected footprint directly matches the named place.
2. **Follow ghosts into representation.** `encounter:056` (p. 72) concerns legends and precise polygons; this is a cartographic move, not a generic US match.
3. **Move to Historic Filipinotown / participation.** `encounter:058` (p. 77) opens PDub’s community inquiry; the route is project/conceptual, not another Ethington quotation.
4. **Follow to an old redlining map.** `encounter:064` (p. 82) joins media practice, historical depth, and a named 1939 Los Angeles map. It has direct place evidence and a cartographic activation.

## Generic map: Wellington, 1924

The current Map Graph contains `map:234`, *Map of Wellington* (1924). The book has no direct Wellington entity or spatial affordance, so the system must not display a “nearby” national quotation.

1. **Core / select 1924 layer.** Offer a source about thick maps and temporal layers (`encounter:014`, p. 17) because the visitor has enacted layered historicity.
2. **Align its raster to the contemporary basemap.** Offer the p. 110 turtle/georeferencing source (`encounter:157`) or p. 111 relation source (`encounter:158`). Label the cause as cartographic resonance.
3. **Zoom sharply outward.** Offer `encounter:075` (p. 97), on the view when zooming out of Google Maps. This is a scale/cartographic relationship, not Wellington scholarship.

The system may instead remain silent if those passages are cooling down or if the state match is weak.

## Book-led cross-geographic route

1. **A historic map raster is aligned with a basemap.** Canonical turtle/georeferencing encounter (`encounter:157`, p. 110) opens the concept `georeferencing`.
2. **Follow representation / accuracy.** `encounter:093` (p. 124) concerns the choices built into accuracy and representation.
3. **Follow participation / witnessing.** `encounter:036` (p. 43) asks about listening to, mapping, and amplifying voices in Cairo; the move is conceptual/project-led, not based on map proximity.
4. **Follow event / witness.** Canonical `encounter:160` (p. 203), “The event remains,” leads to the Tohoku gallery. Here a geographically distinct place is opened through the book’s event, witnessing, and archive network.

Every crossing in this path requires a stored reason: direct source relationship, book-inferred co-articulation, or editorial resonance. Geographic distance never gets silently turned into scholarly equivalence.
